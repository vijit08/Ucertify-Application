<script>
  export let type = "button";
  export let caption;
  export let href = null;
  export let mode = null;
  export let color = null;
  export let style = null;
  export let disabled = false;
</script>

{#if href}
  <a {href}>{caption}</a>
{:else}
  <button class="{mode} {color}" {type} {style} {disabled} on:click
    >{caption}</button
  >
{/if}

<style>
  button,
  a {
    font: inherit;
    border: 1px solid #cf0056;
    background: #cf0056;
    padding: 0.5rem 1rem;
    color: white;
    border-radius: 5px;
    box-shadow: 5px 5px 5px rgba(0, 0, 0, 0.26);
    cursor: pointer;
    text-decoration: none;
    margin-top: 20rem;
    font-weight: 700;
  }

  button:focus {
    outline: none;
  }

  button:hover,
  button:active,
  a:hover,
  a:active {
    font: inherit;
    border: 1px solid #cf0056;
    background: pink;
    padding: 0.5rem 1rem;
    color: black;
    border-radius: 5px;
    box-shadow: 5px 5px 5px rgba(0, 0, 0, 0.26);
    cursor: pointer;
    text-decoration: none;
    margin-top: 20rem;
    font-weight: 700;
  }

  button:disabled,
  button:disabled:hover,
  button:disabled:active {
    background: #ccc;
    border-color: #ccc;
    color: #959595;
    box-shadow: none;
    cursor: not-allowed;
  }

  .success {
    background: #01a129;
    border-color: #01a129;
  }

  .success:hover,
  .success:active {
    background: #1ac745;
    border-color: #1ac745;
  }

  .outline {
    background: transparent;
    color: #cf0056;
    box-shadow: none;
  }

  .outline:hover,
  .outline:active {
    background: #ffc7de;
    box-shadow: none;
  }

  .outline:disabled,
  .outline:disabled:hover,
  .outline:disabled:active {
    background: transparent;
    color: #ccc;
  }

  .outline.success {
    border-color: #01a129;
    color: #01a129;
  }

  .outline.success:hover,
  .outline.success:active {
    background: #c2ffd1;
  }
</style>
